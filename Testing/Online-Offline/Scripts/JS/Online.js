if (!navigator.onLine) {
    document.body.innerHTML = "<h1>No network found | 404</h1>";
    document.body.innerHTML = "<h2>Reload the page once connected<h2>";
    document.body.innerHTML = "<h2>If you are connected then report this bug here: https://github.com/HttpAnimation/FYC-Rewrite-V2/issues"
}